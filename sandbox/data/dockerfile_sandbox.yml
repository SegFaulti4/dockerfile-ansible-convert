- hosts: all
  name: Generated from dockerfile
  tasks:
  - shell:
      cmd: echo "deb http://shadowsocks.org/debian wheezy main" >> /etc/apt/sources.list
  - apt-get:
      force-apt-get: 'yes'
      update_cache: 'no'
  - apt-get:
      force: 'yes'
      force-apt-get: 'yes'
      name:
      - shadowsocks-libev
